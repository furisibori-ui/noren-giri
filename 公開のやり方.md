# THE NOREN GIRI をネットに公開する手順

素材を入れ終わったら、次の2ステップで誰でも遊べるURLを用意できます。

1. **GitHub にアップロード**（コードと素材をネット上に保存）
2. **Vercel で公開**（URLを作ってブラウザで開けるようにする）

---

## 次にやること（今どこ？）

| 状態 | 次にやること |
|------|----------------|
| **フォルダはまだアップロードしてない** | 下の「いまやること」のとおり、ターミナルで **1-2 と 1-3** をやる（手元の noren-giri を GitHub に送る） |
| GitHub で「空のリポジトリ」だけ作った | 同じ。**1-2** 全部 → **1-3** の3行で、フォルダの中身をそのリポジトリに送る |
| `git push` まで成功した（GitHub にファイルが見える） | **ステップ2**。Vercel で noren-giri を Import → Deploy |
| Vercel で Deploy した | 完了。**Visit** のURLがゲームの住所 |

**「微妙」なとき** … GitHub で https://github.com/furisibori-ui/noren-giri を開いて、中にファイル（index.html や assets など）が **何もない or README だけ** なら「まだフォルダはアップロードしてない」なので、**1-2 と 1-3 をやる** でOKです。

---

## 事前に用意するもの

- **GitHub のアカウント** … まだなら [github.com](https://github.com) で「Sign up」から無料で作成
- **Vercel のアカウント** … [vercel.com](https://vercel.com) で「Sign Up」→「Continue with GitHub」で GitHub 連携がおすすめ
- **ターミナル（コマンドを打つ場所）** … Mac なら「ターミナル」アプリ、Windows なら「コマンドプロンプト」や「PowerShell」

---

# ステップ1：GitHub にアップロード

**方法A：GitHub Desktop を使う**  
**方法B：ターミナルでコマンドを打つ**  
**方法C：GitHub のウェブでフォルダごとアップロードする（アプリもターミナルも使わない）**

---

## 方法A：GitHub Desktop で push する（簡単）

1. **GitHub Desktop をインストール**  
   https://desktop.github.com/ からダウンロードしてインストール
2. **GitHub にログイン**  
   起動後「Sign in to GitHub.com」で、ブラウザが開いたら GitHub にログインして許可
3. **noren-giri フォルダを追加**  
   - メニュー **File** → **Add Local Repository**  
   - **Choose...** で `/Users/d18212/Desktop/plan02/noren-giri` を選ぶ  
   - 「This directory does not appear to be a Git repository」と出たら **「create a repository」** は選ばず、いったんキャンセルして、先にターミナルで `cd noren-giri` のあと `git init` だけ実行してからもう一度 Add Local Repository
4. **リモートがまだなら設定**  
   画面上で「Publish repository」や「Set up remote」のような表示があれば、  
   - Repository name: `noren-giri`  
   - すでに GitHub に空の noren-giri を作ってある場合は、**Repository** メニュー → **Repository settings** → **Remote** で `origin` の URL が `https://github.com/furisibori-ui/noren-giri.git` になっているか確認
5. **Push**  
   左上の **「Push origin」** をクリック（トークン入力なしで push できる）

これで GitHub にコードが上がります。続けて **ステップ2（Vercel で公開）** へ。

---

## 方法C：GitHub のウェブでフォルダごとアップロードする

（GitHub Desktop もターミナルも使わず、ブラウザだけで上げる）

1. ブラウザで **https://github.com/furisibori-ui/noren-giri** を開く
2. **「Add file」** をクリック → **「Upload files」** を選ぶ
3. **noren-giri フォルダの中身** をまとめて送る：
   -  Finder で `/Users/d18212/Desktop/plan02/noren-giri` を開く
   -  **中身のファイル・フォルダをすべて選択**（⌘+A）して、そのまま GitHub の「Drag files here」のエリアに **ドラッグ＆ドロップ**
   -  または「choose your files」から、noren-giri の中のファイルをまとめて選択
4. 下の **Commit message** に「THE NOREN GIRI 初回アップロード」などと入力
5. **「Commit changes」** をクリック

※ フォルダ（assets など）は、中身のファイルを入れれば GitHub 上でフォルダ構造になります。**assets/images** のファイルと **assets/sounds** のファイルをそれぞれアップロードすればOK。いったん「assets」フォルダを作れない場合は、先に「Upload files」で index.html など直下のファイルだけ上げてから、もう一度「Add file」→「Upload files」で assets 内のファイルを上げてもよい。

これで GitHub にコードが上がります。続けて **ステップ2（Vercel で公開）** へ。

---

## 方法B：ターミナルで push する

---

## 1-1. GitHub で新しいリポジトリを作る

1. ブラウザで **https://github.com** を開き、ログインする
2. 右上の **「+」** をクリック → **「New repository」** を選ぶ
3. 次のように入力する：
   - **Repository name** … 例：`noren-giri` や `the-noren-giri`（好きな名前でOK）
   - **Public** を選ぶ
   - **「Add a README file」** は **つけなくてOK**（あとから自分で push するので）
4. **「Create repository」** をクリックする
5. 次の画面で **2種類のコマンド** が表示される：
   - **「…またはコマンドラインで新しいリポジトリを作成する」** … これは **使わない**（noren-giri はもう手元にあるので）
   - **「…またはコマンドラインから既存のリポジトリをプッシュする」** … **こちらの3行を使う**
   
   **THE NOREN GIRI 用（furisibori-ui / noren-giri）のコマンド：**
   ```bash
   git remote add origin https://github.com/furisibori-ui/noren-giri.git
   git branch -M main
   git push -u origin main
   ```
   この **3行を 1-3 で順に打つ**（すでに git init と commit まで済ませたあと）

---

## 1-2. 自分のPCで「noren-giri」フォルダを Git の管理下にする

### 「Git のリポジトリにする」「git init」って何？

- **Git** … ファイルの「変更履歴」を残して、あとから戻したり GitHub に送ったりするためのツール
- **リポジトリ** … その「履歴を管理する場所」＝「このフォルダを Git で管理するよ」という箱
- **git init** … 「今いるフォルダを、その箱（リポジトリ）にします」という **1回だけ** の準備コマンド

やることは「ターミナルで noren-giri の中に入って、`git init` と打って Enter」だけです。

---

### 手順

1. **ターミナル** を開く  
   - Mac：Spotlight（⌘+スペース）で「ターミナル」と入力 → 「ターミナル」アプリを起動  
   - 黒い（または白い）画面に文字が並んでいればOK
2. **noren-giri フォルダまで移動** する：
   ```bash
   cd /Users/d18212/Desktop/plan02/noren-giri
   ```
   （plan02 が別の場所にある場合は、そのパスに書き換える）
3. **このフォルダを Git のリポジトリにする**（ここが「git init」）  
   - ターミナルに **半角で** 次のように打つ：  
     `git init`  
   - **Enter** を押す  
   - 成功すると、次のような英文が1行出る：  
     `Initialized empty Git repository in /Users/d18212/Desktop/plan02/noren-giri/.git`  
   - これで「noren-giri が Git のリポジトリになりました」という意味。**一度だけ** やればOK
4. 全部のファイルを「これから上げるよ」のリストに追加する：
   ```bash
   git add .
   ```
5. ひとまとまりの保存として記録する（コメント付き）：
   ```bash
   git commit -m "THE NOREN GIRI 初回アップロード"
   ```
   「X files changed」のような表示が出ればOK

---

## 1-3. GitHub のリポジトリとつなげて、アップロードする

1. ターミナルで、**noren-giri フォルダにいる状態**で、次の3つを **1行ずつ** 実行する：
   ```bash
   git remote add origin https://github.com/furisibori-ui/noren-giri.git
   ```
   ```bash
   git branch -M main
   ```
   ```bash
   git push -u origin main
   ```
2. **補足**：上は「furisibori-ui / noren-giri」用。別のユーザー名・リポジトリ名で作った場合は、1行目のURLだけそのリポジトリのURLに書き換える
3. **「Author identity unknown」が出た場合**（初回によくある）  
   コミットが作れていないので、**先に名前とメールを登録**してから、もう一度コミットする：
   ```bash
   git config --global user.name "furisibori-ui"
   git config --global user.email "あなたのメール@example.com"
   ```
   （メールは GitHub に登録しているアドレス、または GitHub の **Settings → Emails** にある `xxx@users.noreply.github.com` でもOK）  
   登録したら：
   ```bash
   git add .
   git commit -m "THE NOREN GIRI 初回アップロード"
   git push -u origin main
   ```
4. **push で「Password」が聞かれたら** トークンを使う（通常のパスワードは使えない）。  
   **ターミナルでパスワード入力がうまくいかない場合** は、次の「トークンをURLに入れる方法」を使う：
   - GitHub で **Settings → Developer settings → Personal access tokens** からトークンを作成してコピー
   - ターミナルで（`YOUR_TOKEN_HERE` をコピーしたトークンに置き換えて）実行：
     ```bash
     git remote set-url origin https://furisibori-ui:YOUR_TOKEN_HERE@github.com/furisibori-ui/noren-giri.git
     ```
   - 続けて `git push -u origin main` を実行（パスワード入力なしで送れる）
   - 成功したら、トークンをURLから外す：  
     `git remote set-url origin https://github.com/furisibori-ui/noren-giri.git`
5. 成功すると、ターミナルに「Branch 'main' set up to track...」のような表示が出る
6. **GitHub のリポジトリのページを再読み込み** すると、index.html や assets フォルダなどが一覧に出ていれば **アップロード完了**

---

# ステップ2：Vercel で公開（URL を作る）

## 2-1. Vercel に GitHub を連携する（初回だけ）

1. ブラウザで **https://vercel.com** を開く
2. **「Sign Up」** または **「Log In」** をクリック
3. **「Continue with GitHub」** を選ぶ
4. 表示に従って GitHub の認証を許可する（「Authorize Vercel」など）
5. これで「GitHub のリポジトリを Vercel で公開できる」状態になる

---

## 2-2. プロジェクトとして「noren-giri」を読み込む

1. Vercel のダッシュボードで **「Add New…」** → **「Project」** をクリック
2. **「Import Git Repository」** の一覧から、さっき push した **noren-giri（または付けたリポジトリ名）** を探す
3. 見つかったら **「Import」** をクリック
4. **「Configure Project」** の画面になる：
   - **Root Directory** … **「Edit」** を押して、もし `noren-giri` のようなサブフォルダだけを公開したい場合はそこを指定する。**noren-giri フォルダだけを GitHub に上げている場合は、ここは空のままでOK**
   - **Framework Preset** … 「Other」のままでOK
   - その他はそのままでOK
5. **「Deploy」** をクリックする

---

## 2-3. デプロイ完了まで待つ

1. 「Building…」「Deploying…」のようなログが流れる（数十秒〜1分程度）
2. **「Congratulations!」** のような画面になり、**「Visit」** や **「〇〇.vercel.app」** のリンクが出たら **公開完了**
3. そのリンクをクリックすると、**THE NOREN GIRI のメニュー画面**が開く
4. この **〇〇.vercel.app** のURLが「ゲームの住所」なので、このURLを共有すれば誰でも遊べる

---

# このあとどうするか

- **URLを変えたい・自分用のドメインにしたい**  
  → Vercel のプロジェクトの **Settings → Domains** で設定できる
- **素材やコードを直したら**  
  → 同じ noren-giri フォルダで編集してから、もう一度：
  ```bash
  cd /Users/d18212/Desktop/plan02/noren-giri
  git add .
  git commit -m "説明を書く（例：BGMの音量調整）"
  git push
  ```
  push すると Vercel が自動で再デプロイし、数分で新しい内容が反映される
- **学校のゲームと同じように**  
  → この「公開のやり方」は「noren-giri フォルダを1つのリポジトリとして GitHub に上げ、Vercel でそのリポジトリをデプロイする」手順です。学校のゲームで「フォルダごとリポジトリにして Vercel で出した」のと同じ流れです

---

# よくあるつまずき

| 症状 | 対処 |
|------|------|
| `git: command not found` | Mac なら Xcode Command Line Tools を入れる（ターミナルで `xcode-select --install`）。Windows なら [git-scm.com](https://git-scm.com) から Git をインストール |
| `Permission denied` や `Authentication failed` | GitHub の認証（パスワード or Personal Access Token）が間違っている。GitHub の設定でトークンを再発行して試す |
| push は成功したのに Vercel にリポジトリが出ない | Vercel の「Import」画面で「Adjust GitHub App Permissions」などが出たら、そこでリポジトリへのアクセスを許可する |
| サイトを開いても真っ白・素材が表示されない | ブラウザの開発者ツール（F12）の「Console」でエラーを確認。素材のファイル名（小文字・拡張子）が 素材の入れ方.txt と一致しているか確認する |
| **ゲーム画面だけ黒い（音は出る）** | 下の「背景が黒いときの確認（Mac）」を参照 |

---

## 背景が黒いときの確認（Mac）

ゲームを開いて「スタート」を押したあと、背景が真っ黒のままのときに、画像が読み込めているか確認する手順です。

### 1. 開発者ツール（コンソール）を開く

- **Chrome / Edge**
  1. ゲームのページを表示したまま、キーボードで **`Option + Command + J`**（オプション + コマンド + J）を押す  
     （または メニュー **表示** → **デベロッパー** → **JavaScript コンソール**）
  2. 画面の下か横に「Console」というタブのパネルが開く

- **Safari**
  1. まず **Safari** → **設定（環境設定）** → **詳細** で「メニューバーに"開発"メニューを表示」にチェックを入れる
  2. ゲームのページを開いた状態で、メニュー **開発** → **Webインスペクタを表示**（または **JavaScript コンソールを表示**）
  3. 下に出たウィンドウで **コンソール** タブを選ぶ

- **Firefox**
  1. **`Option + Command + K`**（オプション + コマンド + K）を押す  
     （または メニュー **ツール** → **ブラウザーツール** → **Web コンソール**）
  2. 下に「コンソール」パネルが開く

※ Mac では **F12** は多くのブラウザでコンソールを開かないので、上記のショートカットを使います。

### 2. ネットワークで画像が読めているか確認（いちばん確実）

1. 開発者ツールを開いた状態で、上にあるタブから **「Network」**（ネットワーク）を選ぶ
2. ページを **再読み込み**（**Command + R**）する
3. 一覧で **「background」** や **「images」** でフィルタするか、一覧をスクロールして `background.png` を探す
4. その行をクリックして選択し、右側の **Status** を確認  
   - **200** → 正常に取得できている（それでも黒い場合は別原因）  
   - **404** → ファイルがない（パスやファイル名・GitHub に上げたか確認）  
   - **Failed** / 赤い行 → 読み込み失敗

### 3. コンソールでエラーがないか確認

- 開発者ツールの **「Console」** タブを開く
- 赤い文字の **Error** や **404** が出ていないか見る
- 出ている場合は、そのメッセージをそのままコピーして教えてもらえると原因を絞りやすいです

---

ここまでで「まずアップロード」→「GitHub に push」→「Vercel で Deploy」まで一通りできます。どこかで表示やメッセージが違う場合は、その画面の名前やメッセージを教えてもらえれば、その部分だけ詳しく案内できます。
